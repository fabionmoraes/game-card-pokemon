import styled from 'styled-components';

export const Button = styled.button`
  border: 1px solid #e8af1f;
  border-radius: 4px;
  background: none;
  color: #eee;
  padding-left: 16px;
  padding-right: 16px;
  padding-top: 4px;
  padding-bottom: 4px;
  font-size: 22px;
  transition: background-color 0.2s;

  &:hover {
    background: #e8af1f;
  }
`;
